{{!< default}} <section class="max-w-7xl mx-auto px-4 md:px-8 py-20 my-12">

    <!-- Posts Grid -->
    {{> "post-grid" title=(concat "More from " tag.name)}}

    {{#if pagination}}
    <div class="mt-16 flex justify-center">
        {{pagination}}
    </div>
    {{/if}}
    </section>