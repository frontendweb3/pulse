{{!< default}} <section class="max-w-7xl mx-auto px-4 sm:px-6 py-12 md:py-20">

    <!-- Posts Grid -->
    {{> "post-grid"}}

    <!-- Pagination -->
    {{#if pagination}}
    <div class="mt-16 flex justify-center">
        {{pagination}}
    </div>
    {{/if}}
    </section>